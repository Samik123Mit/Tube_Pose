# Notebooks

Drop exploratory notebooks here. Suggested starters:

- `eda.ipynb`: distribution of bbox sizes, angle histogram per image, per-image tube count.
- `error_analysis.ipynb`: load `outputs/evaluation/metrics.json`, plot worst-case predictions.
- `inference_demo.ipynb`: single-image inference with `inference.run_inference` and side-by-side rendering.
